网格重采样的后果
------------------

在对网格进行重采样或重新取样时，会使用多种算法（参见 **-n**），
这些算法可能导致重采样后的数值出现一定的失真或意外结果。
使用样条函数进行重采样的一个常见现象是：新的采样值可能会略微超出原始网格的全局最小值或最大值。
如果这种超出范围的现象不可接受，可以在 **-n** 选项中添加 **+c** 参数，
以对重采样结果进行裁剪，从而确保输出的数值不会超过输入网格的最小值和最大值。
