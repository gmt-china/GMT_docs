-r 选项
=======

GMT 中的网格文件有网格线配准和像素配准两种配准方式，详情见
:ref:`grid-registration` 一节。

默认情况下，GMT认为输入/输出的网格文件都采用网格线配准方式。
使用 ``-r`` 选项，则GMT会认为输入/输出的网格文件为像素配准方式。

对于相同的网格区域（\ ``-R``\ ）和网格间隔（\ ``-I``\ ）而言，
像素配准会比网格线配准少一列和一行数据。
