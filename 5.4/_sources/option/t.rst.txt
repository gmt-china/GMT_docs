.. index:: !transparency

-t 选项
=======

``-t`` 选项用于设置\ **当前图层**\ 的透明度，其语法为::

    -t<transp>

其中 ``<transp>`` 表示透明度，可以取0到100内的数字。
默认值为0，即不透明，50表示半透明，100则表示完全透明。

PS 格式不支持透明效果。在使用 ``-t`` 选项后，PS文件中看不到透明效果，
需要将PS格式转换为PDF、PNG等格式中才能看到效果。

使用如下命令将 PS 文件转换为 PDF::

    gmt psconvert -A -P -Tf xxxx.ps
